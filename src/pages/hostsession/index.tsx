'use client';
// Update the import path below to the correct location of WebinarForm, for example:
import SessionForms from "@/components/Forms/SessionForms";

export default function HostSessionForms() {
return (  
<div className="min-h-screen bg-white">
    <div className="max-w " >
        <SessionForms />
        </div> 
        </div>
 )
}
